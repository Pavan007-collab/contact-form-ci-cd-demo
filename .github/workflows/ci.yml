name: Contact Form CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Display Repository Structure
        run: ls -R

      - name: CI Success
        run: echo "🎉 GitHub Actions is working successfully!"